import { CompletedStateEnum } from '../completed-state.enum';

export interface VisualState{
  introduction: CompletedStateEnum;
  fontSize: CompletedStateEnum;
  contrast: CompletedStateEnum;
  finalQuiz: CompletedStateEnum;
}
